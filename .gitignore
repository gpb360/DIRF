# Node
node_modules/

# DIRF local working artifacts (central store lives in ~/.dirf/, not the repo)
.dirf/
.dirf.migrating.*/

# Editor / agent session state (local only)
.zcode/
.claude/

# Logs (machine output — never tracked)
*.log

# Legacy generated runs; new target-owned runs live under each target's .dirf/attempts/
workflows/user/
*.html
/graphify-out/

# Private recording assets (scripts stay text-only in Git)
/docs/marketing/dirf-youtube-launch/video-production/**/assets/audio/
/docs/marketing/dirf-youtube-launch/video-production/**/.media/audio/
/docs/marketing/dirf-youtube-launch/video-production/**/.waveform-cache/

# rtk runtime state (machine-generated command history; never tracked)
/rtk/

# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
